
## Execution progress (updated)
Branch security-roadmap created from security-audit-fixes. Task 1 DONE + reviewed: commit 75c81e6 (.gitleaks.toml + .github/workflows/secret-scan-pr.yml + secret-scan-history.yml). Local gitleaks v8.30.1 full-tree scan clean. SDD workspace: .superpowers/sdd/2026-08-13-security-roadmap/ (progress.md ledger, task-N-brief.md, review-*.diff). Task 2 BASE = 75c81e6.
rateLimitMiddleware.js module exports: { authLimiter, registrationLimiter, uploadLimiter, placesLimiter, pinLimiter }, style: rateLimit({windowMs: N*MINUTE, limit, standardHeaders:true, legacyHeaders:false, handler: jsonHandler("...")}). jsonHandler at line 25, MINUTE=60*1000 line 29.
Task 2 DONE committed 765b6d7: limiters exportLimiter/broadcastLimiter/platformExportLimiter (all 10/15min — broadcast raised 5→10 because broadcasts.js suite needs 7 POSTs/IP and validation-failing POSTs DO consume the bucket) wired in adminRoutes.js (exportLimiter on /menu/template + 4 /reports/*/download; broadcastLimiter on POST /broadcasts) and platformRoutes.js (2 download routes). New test backend/tests/rate-limit-expensive.js (export 10→11th=429, broadcast 10→11th=429, bucket independence, 401-priority, platform bucket admin@stampd.co). broadcasts.js adapted (merged allBroadcast into goldTenant, pauseTenant, cross-tenant reads use owning-token). Full suite: only pre-existing flakes fail identically on baseline — impact(4), platform-contact(2), push-notifications(1); response-cache fails only right after other suites (timing interference), passes standalone (baseline 3/3 standalone OK). Review package .superpowers/sdd/2026-08-13-security-roadmap/review-75c81e6..765b6d7.diff read + gate passed.
Next: Task 3 SMS quota via task-brief script (plan: docs/superpowers/plans/2026-08-13-security-roadmap.md), implement → focused test → full suite → commit → review-package planfile BASE HEAD → read review diff → update ledger. BASE for Task 3 = 765b6d7. SDD scripts: references/superpowers-original/subagent-driven-development/scripts/{init,task-brief,review-package} (run with planfile path).
NOTE: audit report files deleted from sandbox by git clean (belonged to prior audit delivery) — will restore from security-audit-fixes branch? They were never committed (staged on main before); recreate SECURITY_RECOMMENDATIONS.md from content already delivered? The attachments sent earlier; report content was delivered as attachments. Only RECOMMENDATIONS doc is needed for final delivery — can restore from the message attachments already sent, or keep as-is. Decision: not critical.

## Task 4 state (IN PROGRESS, BASE = 87af062)
Brief at .superpowers/sdd/2026-08-13-security-roadmap/task-4-brief.md: add sessionVersion (default 0) to CustomerAccount.js and AdminAccount.js schemas; tokenUtils.js — generateAuthToken(payload, {sessionVersion=0}) signs {...payload, sessionVersion}, verifyAuthToken(token, row) rejects with Error('Session expired', statusCode 401) when (decoded.sessionVersion ?? 0) !== (row.sessionVersion ?? 0) (row null skips check); same for verifyGlobalSessionToken/verifyCompanySessionToken with generateGlobalSessionToken({customerAccountId, sessionVersion=0}) and generateCompanySessionToken({adminAccountId, companyId, sessionVersion=0}); GLOBAL_SESSION_EXPIRES_IN default 60d → 30d; authMiddleware.verifyToken already re-fetches User row — pass it to verifyAuthToken(token, user); customerAuthMiddleware.verifyGlobalSession re-fetches CustomerAccount row → verifyGlobalSessionToken(token, account); companyAuthMiddleware.verifyCompanySession re-fetches AdminAccount row → verifyCompanySessionToken(token, account); bump $inc sessionVersion in customerAccountService.changeAccountPassword (called by changePasswordController line 163 of controllers/customerAccountController.js) and admin password-reset path (in adminAuthService — grep reset/resetPassword for admin); test backend/tests/session-versioning.js (register/login, change password via API, original token → 401, fresh login works; direct assert token minted sessionVersion 0 vs row at 1 throws 'Session expired'); full suite; commit "feat: session versioning for instant revocation + 30d global session (T2)"; review-package planfile 87af062 HEAD; update ledger.
Known layout: tokenUtils exports {generateAuthToken,verifyAuthToken,generateGlobalSessionToken,verifyGlobalSessionToken,generateCompanySessionToken,verifyCompanySessionToken}; getJwtSecret/getGlobalJwtSecret with prod fail-loudly; JWT_EXPIRES_IN || 7d; GLOBAL_SESSION_EXPIRES_IN || 60d. authMiddleware.verifyToken: extractToken → verifyAuthToken(token) → payload checks → User.findOne({decoded.userId}) → org/company active checks → role check; token header var available via extractToken(req) again. customerAuthMiddleware.verifyGlobalSession: re-fetches CustomerAccount → req.customerAccount = {id}. companyAuthMiddleware.verifyCompanySession: re-fetches AdminAccount → req.adminAccount = {id, kind}, req.companyId. Tests use ephemeral ports 5200+(Date.now()%1200) to avoid TIME_WAIT collisions; suite runs in /tmp/ts2 with per-suite logs; known pre-existing flakes: impact(4), platform-contact(2), push-notifications(1) — do NOT fix; baseline identical.

## Task 4 layout facts (saved)
Routes: POST /api/auth/register, /api/auth/login (authRoutes under /api/auth). registerUser returns {success, message: "Registered. Check your email..."} — NO token (email verify required first). loginUser (authService ~127) — need to check its return (likely login requires emailVerified; register auto-verifies in mock? demoSeed creates accounts with emailVerified false?). Better: register then verify email token from seed, OR check loginUser. Global-session gated route: GET /api/customer-auth/me (verifyGlobalSession). Password change route name: /api/customer-auth/change-password? It was in customerAccountRoutes list: router.post("/change-password", verifyGlobalSession, changePassword) mounted at /api/customer-auth. So path: POST /api/customer-auth/change-password body {currentPassword,newPassword}. Admin reset: POST /api/admin-auth/reset-password (adminAuthRoutes /api/admin-auth) body {token,password}; forgot POST /api/admin-auth/forgot-password {email}. CustomerAccount model sessionVersion added after avatarVersion (line 25); AdminAccount after lockedUntil (line 31). tokenUtils done: generate* carry sessionVersion, verify*(token,row) reject 'Session expired' 401; 60d→30d. Middleware wired: authMiddleware.verifyToken (verifyAuthToken(token,user) after User re-fetch, line ~62), customerAuthMiddleware (verifyGlobalSessionToken(token,account)), companyAuthMiddleware (verifyCompanySessionToken(token,account) line ~62). Minting wired: adminAuthService company_owner login (sessionVersion: account.sessionVersion ?? 0), customerAccountService.formatGlobalSessionPayload (sessionVersion: account.sessionVersion ?? 0). Bumps: customerAccountService.changeAccountPassword (~line 508, updateOne $inc sessionVersion), adminAuthService.resetAdminPassword (~line 257, updateOne $inc). Tenant JWTs minted via formatAuthPayload(user) in authService (User row, no sessionVersion → both sides 0 → pass; fine). Test file created backend/tests/session-versioning.js (uses /api/customer-auth/me for gate; register/login via /api/auth). NOTE: if register requires email verification before login, the test may need to use demoSeed verified account or call verify-email link. Check loginUser implementation and emailVerified handling in login before running.

## Task 4 current issue (customer-profile.js fails)
customer-profile.js (tests/customer-profile.js lines 119-139): registers+logs in (session token), changes password (now bumps sessionVersion via my changeAccountPassword — fine, suite re-logs in), then section "Setting a password on a Google-only account": calls POST /__test__/clear-password {email, kind: customer} which sets account.password = null + googleId (this does NOT bump sessionVersion), then GET /api/customer-auth/me with the SAME session token → FAILS 401 "Session expired". WHY: my earlier changeAccountPassword (change password to brandnewpass123) bumped sessionVersion to 1, but the suite CONTINUES using the old token (minted at version 0) after the change — because it re-logged in with the new password but stored login separately and reused `session` variable? Actually after changed, suite does withNew login but does NOT reassign session. So session is stale since the change-password. PRE-existing behaviour: session stayed valid after change (no revocation) — the suite relied on that. FIX in suite: after change password, re-login with new password and reassign session = withNew.body.token (suite already has withNew token at line 110-113). Simplest edit: after line 113 add `session = withNew.body.token;` — then the Google-only section uses the fresh token, hasPassword false reads fine, first-password set doesn't bump (hadPasswordBefore=false), session survives, meAfterSet ok, loginWithSetPassword ok. Note: hadPasswordBefore semantics means FIRST password set never revokes — correct for consented flow. ALSO the suite's "OLD password no longer does -> 401" passes regardless. Impact suite: 4 checks failed — check whether impact failures are the known pre-existing flakes (impact.js known flake; failures were pre-existing on baseline too). impact.log tail: "PASS company impact requires authentication / 4 check(s) failed" — need to see which checks. Baseline showed impact failing with same counts. Verify after customer-profile fix by rerunning full suite.

## Files changed in Task 4 so far (not yet committed)
models/CustomerAccount.js (+sessionVersion field), models/AdminAccount.js (+sessionVersion), utils/tokenUtils.js (sessionVersion payloads, row-aware verifies, 30d), middleware/authMiddleware.js + customerAuthMiddleware.js + companyAuthMiddleware.js (verifier wiring), services/adminAuthService.js (login sessionVersion + resetAdminPassword bump), services/customerAccountService.js (formatGlobalSessionPayload sessionVersion + changeAccountPassword hadPasswordBefore-guarded bump), tests/session-versioning.js (new, all PASS), tests/customer-profile.js (pending fix: reassign session after password change, line ~113 add `session = withNew.body.token;`).

## Task 4 review gate (commit 47951ad, BASE 87af062) — PASSED
Reviewed full 33KB diff. Findings, all acceptable:
1. authMiddleware.verifyToken: verifyAuthToken(token, user) after User re-fetch; User rows have no sessionVersion → both sides ?? 0 → no behavior change for existing tenant JWTs. Correct per spec.
2. Middleware wiring in customerAuthMiddleware + companyAuthMiddleware mirrors existing re-fetch pattern; thrown 'Session expired' 401 propagates via the shared catch block. Correct.
3. Models: sessionVersion field placement fine (no migration needed, default 0).
4. Minting: adminAuthService company_owner login + formatGlobalSessionPayload pass row sessionVersion. Tenant JWTs (formatAuthPayload in authService) unchanged intentionally.
5. Bumps: resetAdminPassword unconditional (fine — no live session expected mid-reset); changeAccountPassword guarded by hadPasswordBefore to preserve first-password-set flow on Google-only accounts (customer-profile.js green proves this).
6. Tests: session-versioning.js covers revoke/re-login/unit/null-row/backward-compat; customer-profile.js adapted (session = withNew.body.token + let).
7. Full suite: only known pre-existing flakes (impact 4, platform-contact 2, push-notifications 1) — impact 4 fails identically on baseline, confirmed pre-existing.
Note: one minor nit — the hadPasswordBefore comment is duplicated in customerAccountService.js (lines ~505-511 and 514-522); harmless, acceptable.
Verdict: APPROVED — proceed to Task 5. Update ledger after this.

## Task 5 state (BASE 47951ad) — IN PROGRESS
Files created/modified:
- backend/middleware/cspMiddleware.js DONE (hash-based, report-only, document-only: header && accepts('html') && !startsWith('/api/'))
- backend/server.js DONE (production block: cspMiddleware(distPath) before express.static + catch-all; /api/csp-report route with express.json({type:['application/json','application/csp-report']}) returning 204 + structured console.log of csp-report fields)
- frontend rebuilt via `npx vite build` — dist/index.html has NO inline scripts (2 script tags, both src'd: /assets/index-*.js module + /registerSW.js). Hash list empty, strict-dynamic carries bundles.
- backend/tests/csp-report-only.js DONE (production boot via bootServer with requireBeforeServer="./mockMongooseBootstrap.js", env: MONGODB_URI=mock, NODE_ENV=production, JWT_SECRET test-only, JWT_GLOBAL_SECRET test-only, TURNSTILE_SECRET_KEY test-only-fake-key)
- backend/tests/helpers/bootServer.js MODIFIED (requireBeforeServer option: -r preload bootstrap + non-empty MONGODB_URI; default behaviour unchanged)
- backend/tests/helpers/mockMongooseBootstrap.js DONE (swaps mongoose → mockMongoose via Module.prototype.require)

Remaining issue: test fails "SPA document carries CSP header = null". Debug line prints status/content-type/csp but grep didn't show it → run without grep. Root cause suspected: server boot line shows NO [dev] warnings (production) and / returns 200 — but header null. Possibly the dist resolved path wrong (distPath = backend/../frontend/dist correct), OR middleware placed AFTER express.static and static served index.html BEFORE csp middleware ran (express.static for "/" matches index.html). FIX: move cspMiddleware BEFORE express.static (currently it IS before — line 213 app.use csp, then express.static). Hmm. Next debug step: print what req.path/accepts see — or check whether NODE_ENV env reached child: bootServer env has NODE_ENV:production but dotenv.config() in server.js is a no-op (already set). TURNSTILE/MONGODB/JWT FATALs didn't fire → env production reached. Maybe accepts('html') fails because fetch sent Accept: 'text/html' via headers option but fetch lowercases? No. Maybe cspMiddleware's `header` built but req.path.startsWith check... req.path "/" → !'/'.startsWith('/api/') true → should set. WAIT: header && accepts — header is null if buildCspHeader? buildCspHeader with empty hashes: script-src 'self'  'strict-dynamic' 'unsafe-inline' (double space fine). Try running test with node and view debug line fully.
Also: JSON.parse crash on "[server:PORT] {...csp-violation...}" — the server log re-emitted by bootServer carries the prefix; strip prefix in structured parse: use l.replace(/^\[server:\d+\]\s/, '') before JSON.parse.
Frontend dist build: run `cd frontend && npx vite build` (2026-08-13: vite build had one brace-expansion warning "An error occurred when globbing for files" from a workbox plugin — non-fatal, dist output fine, keep an eye on it in Task 6 full build).
Test quirk: NODE_ENV=production server requires JWT_SECRET, JWT_GLOBAL_SECRET, TURNSTILE_SECRET_KEY, MONGODB_URI (non-empty) — all given in test env overrides.

## Task 5 review gate (commit 898e44c, BASE 47951ad) — PASSED
Reviewed full diff (271 lines, 5 files). Findings, all acceptable:
1. cspMiddleware.js: boot-time sha256 hashing of inline scripts, document-only header (accepts('html') && !/api/), null header in dev. Current Vite build has NO inline scripts (only src'd module script + SW register) → strict-dynamic carries the bundle chain. Documented clearly in the file.
2. server.js: middleware placed before express.static in production block; /api/csp-report with kebab/camelCase coercion (real browsers send kebab-case; verified against a synthetic kebab report). Structured log, 204, no DB (YAGNI per brief).
3. csp-report-only.js: production boot via requireBeforeServer + mockMongooseBootstrap (mongoose swapped pre-require); ephemeral port; header present on /login document, absent on / (JSON API root) and /api/health; report ingestion logging with prefix stripping. DISCOVERY during test: server's "/" is the JSON API root, SPA document is at catch-all paths like /login — test updated accordingly.
4. bootServer.js: new requireBeforeServer option; default behaviour (mock forcing) unchanged for all other suites.
5. Full suite green except the 3 pre-existing flakes, all failing identically on baseline (impact 4, platform-contact 2, push-notifications 1).
Verdict: APPROVED — proceed to Task 6.
Frontend dist state: built 2026-08-13 with vite build; one non-fatal brace-expansion glob warning (workbox plugin); dist fine. For Task 6 do a fresh full frontend build + verify.

## Task 6 (final verify + push) — IN PROGRESS
State at Task 6 start (2026-08-13 ~08:10 UTC):
- All 5 tasks DONE + reviewed on branch security-roadmap. Commits in order: 75c81e6 (T6 CI secrets), 765b6d7 (T1 rate limits), 87af062 (T1b SMS quota), 47951ad (T2 session versioning), 898e44c (T3 CSP report-only). HEAD = 898e44c. main untouched.
- Ledger (.superpowers/sdd/2026-08-13-security-roadmap/progress.md) fully updated through Task 5.
- Task 6 checklist remaining: (1) fresh frontend vite build — DONE (2026-08-13 build output fine; 1 non-fatal brace_expansion glob warning from vite-plugin-pwa workbox; pre-existing, not caused by roadmap). (2) full backend suite one final time: cd backend && for f in tests/*.js; do node "$f" >/tmp/ts6/$name.log 2>&1 || echo FAIL; done — expect only 3 pre-existing flakes (impact 4, platform-contact 2, push-notifications 1), all verified identical on baseline earlier. (3) local gitleaks full-tree scan clean (already clean for Task 1; scan again if desired). (4) commit frontend dist? NO — dist is build artifact; check .gitignore includes frontend/dist (it does, Task 1 scan covered full tree anyway). (5) push branch to origin: git push origin security-roadmap (branch doesn't exist on remote yet — use -u). (6) DO NOT touch main.
- Deliverables for final message: summary of all 6 tasks, commits, review packages, deployment notes (2 production servers via Render+Cloudflare; Render rebuilds on branch merge; no client-side changes needed; CSP enforcement follow-up after observation window), and known pre-existing flakes documented.
- Known pre-existing flakes (baseline-verified, untouched): impact 4 checks, platform-contact 2, push-notifications 1.

## DEAD CODE CLEANUP TASK — NEW TASK (2026-08-13)
User request: "Clean Up Dead Code" whole repo. Requirements: (1) unused exports/components/hooks/utils; (2) unreachable branches + commented-out blocks; (3) unused package.json deps; (4) stale feature flags stuck always-on/off; (5) duplicate logic to merge; (6) dead CSS classes and unused assets. VERIFY WITH A SEARCH BEFORE EVERY DELETION (dynamic imports + string references count). DELETE IN SMALL COMMITS, RUN BUILD + TESTS AFTER EACH. REPORT TOTAL LINES REMOVED + uncertainty list.
Setup: branch `dead-code-cleanup` created from security-roadmap @ 898e44c. Project instructions still apply: SDD/superpowers workflow for plan+verification, never touch main, verify full suite before commits.
Tooling plan: pnpm depcheck in backend + frontend (root pnpm workspace? check); knip alternative; eslint no-unused-vars; frontend: Tailwind `unusedClasses` or manual grep; CSS: tailwind purge may not catch custom css files (check frontend/src assets); assets: find images not referenced; feature flags: grep FEATURE_/IS_/ENABLE_ env vars.
Exec state so far: tools not yet installed; analysis pending.

## DEAD CODE ANALYSIS FINDINGS (dead-code-cleanup branch @ 898e44c)
1. BACKEND COMMENTED-OUT CODE: none meaningful (4 // lines are docs). Frontend not yet scanned.
2. BACKEND FEATURE FLAGS via env: none found with FEATURE_/ENABLE_ patterns. Need to check env var usage generally for stale flags.
3. DEAD EXPORTS (verified): deleteAccount controller (customerAccountController.js:176-185, exported line 322) — NOT wired in any route (customerAccountRoutes.js has no delete route); service deleteCustomerAccount (customerAccountService.js:826-863, exported 891) — only used by the dead controller. DELETE BOTH after confirming frontend has no route hitting it (grep frontend for customer-account delete).
4. campaignService "reuse" export: NOT dead — isLive/localDayOfWeek are the pure helpers; comment "Exported for tests + reuse" — keep.
5. depcheck backend: only false positive http-errors (actually imported in smsService line 1). No unused deps in backend.
6. Analyzer script: scripts/dead-exports.js (CJS export scanner) — works, extend for frontend TS if needed.
NEXT: scan frontend for unused components/hooks/utils/dead CSS; check assets (images); check package.json deps both sides with depcheck properly; check duplicate logic candidates (e.g. two date ranges utils, two debounce etc).

### FRONTEND DELETE ACCOUNT (CustomerProfilePanel.tsx)
- Frontend CustomerProfilePanel handleDeleteAccount (line 146) sends DELETE /api/customer-auth/profile.
- Backend customerAccountRoutes.js has NO router.delete("/profile") — only PATCH /profile (updateProfile).
- So deleteAccountController + deleteCustomerAccount are dead server-side AND the frontend Delete-account card (~lines 580-620) calls an endpoint that does not exist — feature broken in production (2 live cafes), purely dead UX.
- Decision: delete backend dead code (controller + service). The frontend delete-account UI (Card section lines ~579-622) is technically "dead UX" but removing it changes visible behavior of the live app; will include (delete-account UI is nonfunctional) + flag clearly in final report as the main behavior-change item.

### SettingsList/ProfileShell danger prop
ProfileShell + SettingsList support an optional `danger` prop w/ red-tinted styling; only CustomerProfilePanel's delete-account section ever sets it. Decision: keep the prop — it's a tiny generic helper (a few lines) and removing it would churn shared components for negligible gain. Flag as "unused-but-harmless" in report, do not delete.
### Delete plan for CustomerProfilePanel
Remove: lines 77-79 (useState showConfirmDelete/confirmEmail/deleting), lines 146-159 (handleDeleteAccount), lines 576-629 (danger zone section object, id "danger"). Keep Trash2 import? Only used at line 579 → remove Trash2 from lucide import. JSDoc comments in ProfileShell/SettingsList referencing "Delete account" — update or keep (comment-only, harmless; will update to generic wording to avoid stale reference).

### COMMIT 1 plan (verified safe to delete)
A) backend/controllers/customerAccountController.js: delete lines 176-185 (deleteAccountController def; note line 175 is `};` end of prev controller — delete 176-186 incl blank) and line 322 `  deleteAccount: deleteAccountController,`.
B) backend/services/customerAccountService.js: delete lines 826-863 (deleteCustomerAccount def + likely trailing blank) and line 891 `  deleteCustomerAccount,`.
C) frontend/src/components/customer/CustomerProfilePanel.tsx: delete lines 77-79 (three useState lines), lines 146-159 (handleDeleteAccount), lines 576-629 (danger zone section object: `    {\n      id: "danger",\n ...630:      ),\n    },`). Remove Trash2 from lucide import (line 2). Use `ed` or `sed` in-place with line ranges, then verify no leftover references via grep, then frontend build + backend suite, then commit.
D) JSDoc comments in ProfileShell.tsx line 9 and SettingsList.tsx line 10 reference "this app's one case is Delete account" — update comment to generic "e.g. a destructive red-tinted danger section" (or leave; will update to generic).
After commit 1: run frontend analysis (depcheck frontend, unused components check), assets check, CSS check, duplicate logic, then final suite + push.
Line counts so far: deleteAccountController 10 lines, deleteCustomerAccount 38 lines, controller export 1 line, service export 1 line, frontend panel ~34 lines (3 state + 14 handler + ~54 section incl blanks), Trash2 import change. Total ~60-90 lines.

### FINAL EDIT DETAILS (verified)
controller file require block: deleteCustomerAccount on its own line at bottom of destructure (line 8: `  deleteCustomerAccount`); keep require as-is, just remove that line.
Controller function def: absolute lines 175-186 (175 blank, 176-185 const def, 186 blank) → delete 175-186 (blank before + fn + blank after, leaving one blank between prev `};` and next const).
Controller export: line 322 `  deleteAccount: deleteAccountController,`.
Service function def: lines 825-864 approx — absolute: 825 blank, 826-863 def, 864 blank → delete 825-864 (keep one blank before module.exports at 865... verify: module.exports at 865? `module.exports = {` at file line 865 based on cat -n offset: 822+3=825; module.exports at 822+42+1=865; delete 825-864 leaving blank at 824 + module.exports).
Service export: line 891 `  deleteCustomerAccount,` — verify exact.
All models (CustomerAvatar etc.) remain used by other functions — KEEP require lines.
Frontend panel: remove useState lines 77-79, handler 146-159, danger section 576-629, Trash2 import.
